

<div class="container">
<div class="card" style="width: 18rem;">
  <%= image_tag (url_for(@product.product_image)) ,class:"card-img-top"%>
  <div class="card-body">
    <h5 class="card-title"><%= @product.name%></h5>
    <p class="card-text"><%= @product.description%></p>
  </div>
  

  <div class="card-body">
     <%# <%= link_to "Edit Product", edit_user_product_path(@user), class: 'btn btn-primary' %>  %>
  </div>
</div>
</div>